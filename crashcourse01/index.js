const distance = 30;
const time = 3;
const speed = distance / time;
console.log(speed);
var name = "micayla";
console.log(name);
name = "michelle";
console.log(name);
let isAppleYellow = false;
console.log(isAppleYellow);
let PUBLIC = undefined;
console.log(PUBLIC);
PUBLIC = "government";
console.log(PUBLIC);
var names = "chimela";
console.log(names);
names = "uchechhukwu";
console.log(names);
var x = 6;
var m = 7;
var y = x + m;
console.log(y);
let person = "did obi";
let buy = " bought $6";
let object = " pen ?";
console.log(person + buy + object);
person = "ada did not buy";
object = "house";
console.log(person + object);
const myCar = undefined;
console.log(myCar);
const heIsParalised = true;
console.log(heIsParalised);
let digitarray = [4, 5, 6, 7, 8];
console.log(digitarray);
digitarray = [10, 12, 20];
console.log(digitarray);
const stringarray = "eight,nine,ten,eleven";
console.log(stringarray);
const stringarrayses = ["one,two,three,four,five"];
console.log(stringarrayses);
var isGoodBoy = "false";
console.log(isGoodBoy);
var truthtable = "true,false,true,false";
console.log(truthtable);
isAppleYellow = true;
console.log(isAppleYellow);
let classlist = null;
console.log(classlist);
names = null;
console.log(names);
var workday = undefined;
console.log(workday);
workday = "monday to friday";
console.log(workday);
let workerAgeLimit = 55;
console.log(workerAgeLimit);
let distances = 45;
let diplacement = 5;
let velocity = distances / diplacement;
console.log(velocity);
var vehicle = {
  model: "toyotavenza2020",
  color: "brown",
  tire: 4,
};
console.log(vehicle);
vehicle = {
  company: "bmw",
  model: "bmwxx29",
  reforblished: "on2023",
  tire: true,
  color: "lightgreen",
  types: 340,
};
console.log(vehicle);
const profile = {
  firstname: "chiemela",
  surname: "duru",
  lastname: "daniel",
  age: 18,
};
console.log(profile);
var isInsecuritites = {
  angry: true,
  badMood: false,
  lovegirl: true,
  happymoment: true,
  lovemoney: true,
};
console.log(isInsecuritites);
let number = undefined;
console.log(number);
let letter = null;
console.log(letter);
console.log(number + letter);
letter = "hiamisabella";
console.log(letter);
name = "frank";
let offence = "commttedmurderandsentencedto";
number = 40;
let duration = "yearimprisonment";
console.log(name + offence + number + duration);
name = 2342;
console.log(name);
let book, novel;
book = 37;
novel = "stories";
console.log(book, novel);
novel = null;
book = "empty";
console.log(novel, book);
var business, admin;
business = "financetrading";
admin = "business";
console.log(admin, business);
const p1 = 20;
const v1 = 5;
const p2 = 200;
const v2 = (p1 * v1) / p2;
console.log(v2);
let p = 5;
let b = 6;
let t = 5 * 6;
console.log(t);
const theBus = undefined;
console.log(theBus);
let className = null;
console.log(className);
var currrency = "$";
var amount = 600000;
var motor = "g_wagon";
console.log(currrency + amount + " " + motor);
var boleanarrays = [true, false, false, false, true];
console.log(boleanarrays);
var numberarrs = [3, 4, 2, 6, 7, 8, 10, 18];
console.log(numberarrs);
let stingarr = ["rabit", "rat", "animals"];
console.log(stingarr);
var message1 = "hi baby";
console.log(message1);
var message2 = "i miss u";
console.log(message2);
console.log(message1, message2);
let alert1 = "greetings from nigeria";
console.log(alert1);
let alert2 = "MTN u have been credited with 20000 thank u";
console.log(alert2);
console.log(alert1, alert2);
var heslept = false;
console.log(heslept);
var numbers = 6 * 3;
console.log(numbers);
let celsius = 32;
let fahrenheit = (32 * 9) / 5 + 32;
console.log(fahrenheit);
fahrenheit = 89.6;
celsius = ((89.6 - 32) * 5) / 9;
console.log(celsius);
digitarray = [13, 26, 27, 28, 29, 12, 65, 100];
console.log(digitarray);
var booleanarray = [true, true, true, false];
console.log(booleanarray);
var resultChecked = null;
console.log(resultChecked);
var booklevies = undefined;
console.log(booklevies);
let mySchoolName = "Ndegwu s.s.u.n";
console.log(mySchoolName);
let schoolDetail = {
  college: "winners model academy",
  fund: 2000,
  age: 22,
  approved: true,
};
console.log(schoolDetail);
console.log(letter.split(""));
stringarrayses.push("zero");
console.log(stringarrayses);
let activity = [{ schedule: 1, subject: "biology", time: 12.0 }];
console.log(activity);
activity.beenread = false;
console.log(activity.beenread);
let numeric = 25;
let anwser = numeric + "1";
console.log(anwser);
// daniels visiting his three sons

// daniel distance taking to visit his children
let emekaAndDan = 50;
let buike = 100;
let calculations = emekaAndDan + buike;
let statement = "itTakesDaniel150KmToVisitHisThreeSons";
console.log(emekaAndDan, buike, calculations, statement);

const emekaAndDanielDistance = 50;
const buikeDistance = 100;
const distanceToReachSons = emekaAndDanielDistance + buikeDistance;
const description = `The distance between Emeka and Daniel is ${distanceToReachSons}km.`;

console.log(description);

// step1: amount of gum adriana have=10
// step2:calculate how many gum adrianna hava now?
const adriannaSharedGum = 10;
const fromStore = 3;
const totalGumOwnedByAdrianna = adriannaSharedGum + fromStore;
const gumRemaining = totalGumOwnedByAdrianna - adriannaSharedGum;
const finalAwnser = `the amout of sweet remaining for adrianna is ${gumRemaining}`;

console.log(finalAwnser);

// step1: total amount of candies ashley have=296
// step2:calculate the amount of candies left for ashley=?

const ashleyCandies = 296;
const ashleyGaveMarissa = 105;
const ashleyGaveKayla = 86;
const totalCandiesIssuedOut = ashleyGaveMarissa + ashleyGaveKayla;

const totalCandiesLeft = ashleyCandies - totalCandiesIssuedOut;
const finalAwnsers = `the total amount of candies left for ashley is ${totalCandiesLeft}`;
console.log(finalAwnsers);
const score1 = 56;
const score2 = 10;
console.log(score1 + score2);
console.log(score1 % score2);

let totally = 20;
totally++;
console.log(totally);

let xp = 250;
xp *= 2;
console.log(xp);

let xs = 30;
xs += 22;
xs /= 2;
console.log(xs);

// chizoba is 12 years and her younger brother is 9 year old find out the age gap chizoba used to senior her brother
//step1:giving that chizoba's age= 12
//step2:her brother's age= 9
//step3:calculate the age gap

const chizobaAge = 12;
const theBrotherAge = 9;
const ageGap = chizobaAge - theBrotherAge;
const anwsered = `the age gap between chizoba and her brother is ${ageGap}`;

console.log(anwsered);
console.log(mySchoolName.split(""));

const arithemeticMulttiplication = 25 * 4;
const arithmeticDivision = arithemeticMulttiplication / 5;

const arithemeticAddition = arithmeticDivision + 18;
const arithemeticsubtraction = arithemeticAddition - 7;
console.log(arithemeticsubtraction);
const negativeIntergers = -20 / -4;
console.log(negativeIntergers);

//problem: giving that valueONE=15,valueTwo=3, prove that 15 is less than 3
const value1 = 15;
const value2 = 3;
const prove = value1 < value2;
console.log(prove);

const firstInteger = -4;
const secondInteger = 4;
const solve = `the anwser to this problem is ${firstInteger / secondInteger}`;
console.log(solve);

const PI = 3.14159;
const num = PI.toFixed(3);
console.log(num);
const num2 = PI.toPrecision(4);
console.log(num2);

const lightSpeed = 186000;
const speedExponate = lightSpeed.toExponential();
console.log(speedExponate);

const myName = "duru uche";
const studies = "watching dev dreamer";
console.log(`hi i'm is ${myName} i am watching ${studies} video tutorials`);
console.log(typeof studies);

const maths = 18;
if (maths % 2 === 0) {
  console.log(`${9} is odd number`);
}
const animalscateg = "cat" === "lion";
console.log(animalscateg);
const hospital = "ugoeze eye clinic";
const owner = "Daniel Duru";
console.log(`the owner of ${hospital} is mr ${owner}`);

// assignment: write an of example each, of the operand using comparism
const operandNumber = 18;
const operandNumber2 = 9;
// example1; check if 9 is greater than or equals 18, results: false
console.log(operandNumber2 >= operandNumber, operandNumber >= operandNumber);
// exampple2; check if 9 is <= or equals 18, result : true
console.log(operandNumber2 <= operandNumber, operandNumber <= operandNumber2);
// example3; check result using equality and inequality,
console.log(operandNumber2 === operandNumber, operandNumber2 !== operandNumber);
//example4; declaring a value of thesame type but one has to be number the other string("")
const numbertype = 15;
const numbertype2 = "15";
console.log(numbertype2 == numbertype, numbertype != numbertype2);

// example5; making use of && comparism
const votingNumbersInImo = 6;
const votingNumbersInEkiti = 0;
const candidate = "Perter Obi";

if (votingNumbersInImo && !votingNumbersInEkiti) {
  console.log(`${candidate} won the 2023 presidential election`);
}
// example6; make use of || (OR) comparism, using an IF statement
const candidate2 = "tinibu";
if (votingNumbersInEkiti || votingNumbersInImo) {
  console.log(`${candidate2} and shettima will never be our president`);
}
// example7 the use of negations (!)
let crypto = 20;
crypto = !crypto;
console.log(typeof crypto, crypto);

/*  ##### task from dev dreamer's tutorial = create a function using a function declaration called myName , 
invoke the fuction name to the console .log
2: invoke the function multiples time */
function myDetails() {
  const name = "Duru Daniel .I.";
  console.log("my name is", name);
}
myDetails();
myDetails(); // task2
myDetails();

/* ## task 2 from dev dreamer create a function expression that console.log red,green & orange
2: reassign the color to myColor and use the new variable to invoke the function */

const color = function () {
  console.log("red");
  console.log("green");
  console.log("orange");
  console.log("my colors are red, green, and orange");
};
color();

const myColor = color;
myColor();

// solve a problem that says p1=34, v1=70, t1=20°c+273=293, p2=45, v2=120, t2=?
// find t2 using general gas equation that says p1v1/t1 = p2v2/t2
function physicsProblem(p1, v1, t1, p2, v2) {
  return ((p1 * v1) / t1) * p2 * v2;
}
const t2 = physicsProblem(34, 70, 293, 45, 120);
console.log("the t2 of general gas equation is", t2);

// create a function that prints that a number is even or odd, (this void)
function evenOrOdd() {
  const maths = 50;
  const mathRemain = maths % 2;
  const mathsdivision = maths / 2;
  const oddResult = `${mathsdivision} is an odd number`;
  console.log(oddResult);
  console.log(mathRemain);
}
evenOrOdd();
function calculateNumberOfPen(pen) {
  return pen;
}
const firstPen = 10 * calculateNumberOfPen(5);
console.log(firstPen);

// create an if statement that executes the accurate  age to get married and use else to compare
const marriageAge = 25;
if (marriageAge > 25) {
  // doSomething
  console.log("to get married");
} else {
  // doNotDoSomething
  console.log("don't get married");
}
// assignment from dev dreamer
/*1. what will the following code display in the consol*/
const numb = 10 + 2;
if (numb > 2 && numb < 20) {
  console.log("the condition true");
} else if (numb > 2 && numb < 20) {
  console.log("choose any code to execute");
} else {
  console.log("run if false");
}
/* assignment 2: from dev dreamer, create a variable called user and assign a value called employee. create an if
statement to first check if user is equal to guest , if this is true console.log access denied , then check if user
is equal to employee and console log access allowed */

const user = "employee";
if (user === "guest") {
  console.log("access denied");
} else if (user === "employee") {
  console.log("access allowed");
} else {
  console.log("not a due process");
}

/*task##
1: rewrite the following function as an arrow function : function user(name){
    return `my name is ${name}`
} */
const user1 = (name) => console.log(`my name is ${name}`);

user1("daniel");
/*rewrite the following function as an arrow function const greeting = function(){
    return console.log("hello, how are you ?,")
} */
function greeting(person, greeting) {
  const sayHello = person + greeting;
  console.log(sayHello);
}

greeting("uche said", "good morning");

greeting(5, 10);
const book4 = {
  title: "the felloship of the ring",
  related: ["the two tower, the return of the king"],
  showRelated: function () {
    this.related = "newObject";
  },
};
book4.showRelated();

/*write a javascript program between a a given and 13, if the number is greater than 13 return double the absolute
difference [givenNumber = 30*/

function givingProblem(absoluteDifference) {
  const givenNumber = 30;
  const givenNumber2 = 13;
  const difference = givenNumber - givenNumber2;

  if ((difference) => givenNumber2) {
    console.log(`${difference} is greater than ${givenNumber2}`);
  }
  const doubleAbsolute = absoluteDifference + 17;
  console.log(doubleAbsolute);
}
givingProblem(17);

const siblings = {
  firstSon: "duru emmanuel" + " he is 29 years old",
  secondSon: "duru chukwuebuka" + " he is 27 years old",
  thirdSon: "duru uchechukwu" + " he is 21 years old",
  lastSon: "duru daniel" + " he is 17 years old",
};
console.log(siblings.firstSon);
for (const value in siblings) {
  console.log(`${value} is ${siblings[value]}`);
}

// mama ebuka is a trader she sold a giving item of 100 find the differences between (giving item and non giving item)
// if the non giving item is greater than  100 return double the absolute difference
// step1: give the non value any number of ur choice
// step2: return double if of non value is greater

const mamaEbukaTrader = (nonGivingItem, givingItem) => {
  const differenceItem = nonGivingItem - givingItem;
  if (differenceItem >= givingItem) {
  }
  return differenceItem * 2;
};
const myLastItem = mamaEbukaTrader(250, 100);

console.log(`my last result is ${myLastItem}`);

// write a function that prints hello world
function print(greetings) {
  return "hello world";
}
const print2 = print("i greet");
console.log(print2);

// write a function that returns the square of a number
function squareNum(square) {
  return 40 ** 2;
}
const calc = squareNum(4);
console.log(`the square of 40 is ${calc}`);
// write a function to convert celsius to farenheit
// step1. chose a number of ur choice
// step2. return to a number to convert to farenhiet

const conversion = (celsius) => {
  return (78 * 9) / 5 + 32;
};
const farenheit = conversion(45);
console.log(`78°C in celsius is ${farenheit} in farenheit`);

// write a function that print a given area of a rectangle
// step1. create a variable name "rectangle", asign a length,heigth argument in a parameter of a function
// step1: return to find the exact value of the missing side of a recatngle(width)
const rectangle = (length, heigth) => {
  return length * heigth;
};
const width = rectangle(40, 25);
console.log(`the width is ${width}`);
// write a function named teller fortune
//step1: take four argument : number of children, partners game, geographics location, job tiltle
// step2: output your fortune to the screen like so."you will be like X in y, and married to z with n kids"
// step3: call the function three times with three different values for the ARGUMENT
const fortuneTeller = (
  numberOfChildren,
  partner,
  geographicLocation,
  jobTitle
) => {
  return (
    "u will be a " +
    jobTitle +
    " in " +
    geographicLocation +
    " and married to " +
    partner +
    " with " +
    numberOfChildren +
    " sweet children"
  );
};

const future = fortuneTeller(4, "louisa", "HAMBURG", "Programmer");
console.log(future);
const future2 = fortuneTeller(2, "kemmzy", "london", "Lecturer");
console.log(future2);
const future3 = fortuneTeller(3, "joshua", "Nigeria", "trader");
console.log(future3);

const mapNumber = [45, 34];
const mapNumberResult = mapNumber.map((addMap, value) => {
  return addMap * value;
});
console.log(mapNumberResult);

const fortune = [
  {
    partner: "louisa",
    numberOfChildren: 4,
    geographicLocation: "Hamburg",
    skill: "programmer",
  },
  {
    partner: "kemmzy",
    numberOfChildren: 2,
    geographicLocation: "London",
    skill: "lecturer",
  },
  {
    partner: "joshua",
    numberOfChildren: 3,
    geographicLocation: "Nigeria",
    skill: "trader",
  },
];
for (b = 0; b < fortune.length; b++) {
  const future =
    "u will be a " +
    fortune[b].skill +
    " in " +
    fortune[b].geographicLocation +
    " and u will be married to " +
    fortune[b].partner +
    " with " +
    fortune[b].numberOfChildren +
    " cute children";
  console.log(future);
}
//  do u know how old dog is in human age , what about dogs years? calculate it!
/*step1. write a function named dog age that.
 1. take one argument your puppy age 
 2. calculate the dogs age based on the conversion rate of 1 human year to the year of 7 dogs
 3. output the result to screen like: ur doggie age is RR in years of dogs*/

const convert = (puppyAge, humanAge) => {
  return humanAge / puppyAge;
};
const calculateDogAge = convert(10, 80);
console.log(
  `the conversion rate of 10 dogs to man of 80 years is ${calculateDogAge}`
);

// write a function named calculateSuply
// step1. take to argument: ,amount per day
// step2. calculate the amount consumed by the rest of ur life(based on constant max age)
// step3. output the result to the screen "u will need NN to last for u until the ripe old age of X"
const calculate = (age, amountPerDay) => {
  return 12 * 366;
};
const amountconsumed = calculate(1, 2);
console.log(
  `the amount of bread consumed in 1 year is equal to ${amountconsumed}`
);

// create a function called farenheit to celsius
// return the farenheit using the formular for accurate coversion
// create a variable named "celsius" and assign farenheit to it to return value then console log "celsius"
const farenheitToCelsius = (farenheit) => {
  return ((farenheit - 32) * 9) / 5;
};
console.log(`35°F is equal to ${farenheitToCelsius(35)}°C in celsius`);

// create a function that says big brither naija platform
// step1. pass an argument that says: number of house mates and evicted house mate
// step2. find the difference between number of house mate and evicted house mate
// step3. console log by printing to the screen to show the remaining house mate
const bigBrotherNaijaPlatform = (numberOfHouseMate, evictedHouseMate) => {
  return numberOfHouseMate - evictedHouseMate;
};
const remainingHouseMate = bigBrotherNaijaPlatform(24, 15);
console.log(
  `the remaining house mate in big brother's house is ${remainingHouseMate}`
);

// create a function that says my name pass ur name in return function and call it 2 tumes
function myName1(name) {
  return name;
}
const sayMyName = myName1("daniel chibuzor");
console.log(sayMyName);
function myname2() {
  return "david emmanuel";
}
console.log(myname2());
function myName3() {
  console.log("victoria nicholas");
}
myName3();
//create an object and assign two properties and one objeect method
//step1. let the object be title one and title two
// step2 create an object method and assign function to them

const nameOfNovel = {
  title1: "the return of the king",
  title2: ["to main land", "road to salvation", "up the hill"],
  //print using regular function in the console .log
  books: function () {
    console.log(`the name of the novel i've read is ${this.title1}`);
  },
  arrowfunction: function () {
    this.title2.forEach((relatedNameOfNovel) => {
      console.log(`the name of novels i've read is ${relatedNameOfNovel}`);
    });
  },
  // print by using an arrow function and console log
};
nameOfNovel.books();
nameOfNovel.arrowfunction();

// create an object variable named fruit and assing an object property to then create another object and assign the first to it
const fruit = { type: "pineapple" };
const fruit1 = fruit;
console.log(fruit.type);
console.log(fruit1);
// create an IF statement using the types of equalities to print if the two object above is equal to one another
if (fruit == fruit1) {
  console.log("the are equal");
}
if (fruit === fruit1) {
  console.log("probably i think the are equal");
}
// javascript classes
class cook {
  constructor() {
    this.pot = "new pot";
    this.food = "rice and beans";
  }
  cooking() {
    console.log(`i cooked ${this.food} with my ${this.pot}`);
  }
}
const whatICookedIs = new cook();
whatICookedIs.cooking();

/*create an object of kitchen utensil print to the console each of the uses*/
const utensils = {
  fryingPam: "used for making fries",
  pot: "used for preparing food",
  spatularAndOvum: " used in making cookies like hamburger",
};
for (const value in utensils) {
  console.log(`${value} is ${utensils[value]}`);
}
function sunday(time) {
  return ` at ${time} i wil be meeting my brother`;
}
console.log(sunday("10 am"));

const compare = (equality, inequality) => {
  return equality === inequality;
};
const check = compare(56, "56");
console.log(check);

const compare2 = (equality, inequality) => {
  return equality == inequality;
};
const checkResult = compare2(56, "56");
console.log(checkResult);

/*write a function called temperature check temperature that takes one parameter, temparature , and return a string that tells the user 
whether they should wear a coat or not based onthe temperature if the temperature is below 50 degree, the function should return the 
string "its cold outside u should wear a coat" if the temperature is is between 50 degree and 70,the function should return "its a little b
bit chily outside u might want to bring a coat"if the temperature is above 70 degree the functio should return "its warm outside u 
dont need a coat "*/
const checkTemperature = (temperature1) => {
  if (temperature1 < 50) {
    return "its cold outside u should wear a coat";
  } else if (temperature1 >= 50 && temperature1 <= 70) {
    return "its a little bit chily outside u might want to bring a coat";
  } else {
    return "its warm outside u dont need a coat ";
  }
};

console.log(checkTemperature(50));
/* in this challenge a farmer is asking me to tell him how many legs can be counted amoung all his animals. the farmer breeds three 
species chicken: 2 legs, cow: 4 legs , pigs: 4 legs 
step1. create three argument 
step2. return to add the argument together
step3. console to the screen to print ur out put*/
const farmerPet = (chicken, cow, pigs) => {
  return chicken + cow + pigs;
};
console.log(`the total number of legs in the farm is ${farmerPet(2, 4, 4)} `);

/*create a function that takes the number of wins, draws, and loses then calculate the number of point a football team has obtained
so far win get 3 point,draw get 3 point, losses get 0 point 
step1.create a function called gaolsacquired
step2. create an argument and parameter
step3. print to the screen to show the result*/
const goalAcquired = (win, draw, losses) => {
  return win + draw + losses;
};
console.log("total goal point", goalAcquired(3, 1, 0));

/*Create a function that takes three arguments prob, prize, pay and returns true if prob * prize > pay; otherwise return false.

To illustrate:

profitableGamble(0.2, 50, 9)
 tips: use an if statement to illustrate the codition*/
function profitableGamble(prob, price, pay) {
  if (prob * price > pay) {
    return true;
  } else {
    return false;
  }
}
console.log(profitableGamble(0.2, 50, 9));
/*create a function that returns true if an interger is divisible by 5 and false otherwise
example divide by 5
divide by -55
divide by 37
tips : cretae a function name divisibility the use conditional statement to check the 5 divisibility*/

function divisibility(firstDivisible, secondDivisible, thirdDivisible) {
  if (
    firstDivisible % 5 === 0 ||
    secondDivisible % 5 === 0 ||
    thirdDivisible % 5 === 0
  ) {
    return true;
  }
  return false;
}
console.log(divisibility(-55, 5, 37));
/*a leap year is every four years, so it's a year that is perfectly divisble by four. however if the year is a 
multiple of 100(1800,1900) the must be divisible by 400 
step1: write a function named leapYear to determine if the year is a leap year or not
note: a leap year is 366 i.e a margin of 4 year so multiply 366 by 4*/
function leapYear(everyFourYears) {
  if ((366 * 4) % everyFourYears === 0) {
    return "2020 is a leap year";
  } else if ((366 * 4) % everyFourYears === 0) {
    return "1968 is a leap year";
  } else {
    return "2021 is false";
  }
}

console.log(leapYear(5));

/*a typical car can hold four passenger and one driver, allowing five people to travel around . given n Number of people return how 
cars are needed to seat every one comfortably car needed(5)=1, carsNeeded(11)=3, carsNeeded(0) = 0
step1. add the two previous "passengers" and add together the two previous "cars" that is; pasenger(5+11)=16,cars(1+3)=4
*/
function carsNeeded(passenger, cars) {
  return passenger / cars;
}
console.log(
  "to be comfortably seated it should be",
  carsNeeded(16, 4),
  "passenger per 1 car"
);

/*some basic arithemetics operators are (+,-,/,* and %). in this challenge you will be given three parameters num1 and num2 and an
and an operator, use the operator on number 1 and 2 . example:;
 operate (1,2,"+"), operate(7,10,"-"), operate(20,10,"%")*/
function arithemeticsOperand(num1, num2, symbol) {
  return num1 + num2;
}
console.log(arithemeticsOperand(1, 2, "*"));
function arithemeticsOperand(num1, num2) {
  return num1 - num2;
}
console.log(arithemeticsOperand(7, 10));
let u = 0;
let T = 2;
if (!u && t) {
  console.log("i am chemmzy");
}

/*1. re-write the following if else statement as ternary operators 
let message = ''
if (1 < 10) {
    message = 'condition is true'
} else {
    message = 'condition is false'
}*/
let message7 = "";
message7 = 1 > 10 ? "condition is true" : "condition is false";
console.log(message7);

/*ternary operator : a ternary operator is an easier way of writng an if statement in a simple and understandeable way; and the word ternary means cmposed of three part
which means it is has three operand..... ###task### rewrite the if statement using ternary operator
if (age >= 18) {
    console.log("yes u can vote")}
else {console.log('sorry u can't vote')}..... create a variable called password and assign a value "redrugby"
using a ternary operator NB: when using the ternary , check if password is equal to "redrugby"
let message = ''  if (1 > 10) console.log('condition is true') else { console.log(condition is false)
    create a variable called time and assign a value '12:00' and create a ternary operator to run 
    'good morning':'good evening' dependimg on the time*/
const ageForVoting = 18;
const eligibleAge =
  ageForVoting >= 18 ? "yes u can vote" : "sorry u can't vote";
console.log(eligibleAge);

const password = "redrugby";
const outcome =
  password === "redrugby" ? "successfully unlocked" : "password denied";
console.log(outcome, "u can enter");

const message = "";
const verify = message ? "condition is true" : "condition is false";
console.log(verify);

const timeTik = "12:00";
const timeGreeting = timeTik === "12:00" ? "good morning" : "good evening";
console.log(timeGreeting);
const roll = 4;
switch (roll) {
  case 4:
    console.log("u rolled ten");
    break;
  case 5:
    console.log("u rolled a 5");
    break;
  default:
    console.log(`sorry the number ${roll} u rolled is not available`);
}

/*Imagine you have three numbers: a, b, and c. c is equal to either a or b, but you don't know which one. Your task is to 
create a function that returns whatever number c isn't, out of a and b. So, if c is equal to a, return b, and if c is equal 
to b, return a. Here's what makes this challenge difficult: you cannot use any if statements*/
/*_NB:_NEVER MAKE A TO BE EQUAL TO B */
const swap = (A, B, C) => {
  switch (true) {
    case A === C:
      console.log(B);
      break;
    case C === B:
      console.log(A);
      break;
  }
};
swap(34, 12, 12);
swap(20, 10, 20);
const countries = [
  "america",
  "london",
  "nigeria",
  "ghana",
  ["cameroon", "mexico"],
];
countries[(4, [""])] = "china";
console.log(countries[0]);

const today = new Date().toLocaleString("default", { weekday: "long" });

switch (today) {
  case today === "Mon":
    console.log(`today is ${today}`);
    break;
  case today === "Tue":
    console.log(`today is ${today}`);
    break;
  case today === "Wed":
    console.log(`today is ${today}`);
    break;
  case today === "Thu":
    console.log(`today is ${today}`);
    break;
  case today === "Fri":
    console.log(`todays is ${today}`);
    break;
  case today === "Sat":
    console.log(`today is ${today}`);
    break;
  case today === "Sun":
    console.log(`today is ${today}`);
    break;
  default:
    console.log(`no idea !! `);
    break;
}

const favFood = (cupCake) => {
  switch (cupCake) {
    case "salad":
      return `thats right! ${cupCake} is my favourite food `;

    case "cup cake":
      return `thats right! ${cupCake} is my favourite food`;

    case "spaghetti":
      return `thats right! ${cupCake} is my favourite food`;

    case "green beans":
      return `thats right! ${cupCake} is my favourite food`;

    default:
      return `${favFood} favFood not found`;
  }
};
//const cupCake = "cup cake"
console.log(favFood("cup cake"));

const getDate = new Date(2023, 4, 21, 8, 26);
console.log(getDate);
/* write a javacript programm to display the normal day and time in the following following format . sample 
output: today is : current day . current time is : 30 : 38 */

const displayTime = (Date) => {
  return `current date is : ${Date}`;
};
console.log(displayTime(new Date(2022, 4, 23, 7, 20)));
const colours = ["red", "orange", "green"];
for (let colour = 0; colour < colours.length; colour++) {
  console.log(`${colours[colour]} is my colour i love it`);
}
/*Write a JavaScript program to check whether a matrix is a diagonal matrix or not. In linear algebra, a diagonal matrix 
is a matrix in which the entries outside the main diagonal are all zero (the diagonal from the upper left to the lower 
right). 
Example:
[1, 0, 0], [0, 2, 0], [0, 0, 3] ]) = true
[1, 0, 0], [0, 2, 3], [0, 0, 3] ]) = false
*/
const checkMatrix = (user_matrix) => {
  for (
    let diagonalMatrix = 0;
    diagonalMatrix < user_matrix.length;
    diagonalMatrix++
  ) {
    for (
      let diagonalMatrix2 = 0;
      diagonalMatrix2 < user_matrix.length;
      diagonalMatrix2++
    ) {
      if (
        diagonalMatrix !== diagonalMatrix2 &&
        user_matrix[diagonalMatrix][diagonalMatrix2] !== 0
      )
        return false;
    }
  }
  return true;
};
console.log(
  checkMatrix([
    [1, 0, 0],
    [0, 2, 0],
    [0, 0, 3],
  ])
);
console.log(
  checkMatrix([
    [1, 0, 0],
    [0, 2, 3],
    [0, 0, 3],
  ])
);

/*Write a JavaScript program to convert temperatures to and from Celsius, Fahrenheit.  
[ Formula : c/5 = (f-32)/9 [ where c = temperature in Celsius and f = temperature in Fahrenheit ]
Expected Output :
60°C is 140 °F
45°F is 7.222222222222222°C
_NOTE:_ u will create a function two times then evaluate the conversions*/
const convertToC = (farenheit, celsius) => {
  const parameter = farenheit
    ? farenheit - (32 * 5) / 9
    : (celsius * 9) / 5 + 3;

  return parameter;
};
console.log(convertToC(45, undefined));
console.log(convertToC(undefined, 60));

const convertToF = (celsius) => {
  return (celsius * 9) / 5 + 32;
};
console.log(`65°C is ${convertToF(60)}°F in farenheit`);

/*create a function named speeder and then inserrt an expression thats check if whats is passed to ur parameter argument
is equals to the value passed in a statement when divided and its remainder is zero*/
const speeder1 = (speeding) => {
  for (let iterate = speeding; iterate <= speeding; iterate = iterate + 1) {
    if (iterate % speeding == 2) {
      break;
    }
  }
  return speeding;
};
console.log(speeder1(15));
const mySpeed = 30;

/*Write a function that takes the base and height of a triangle and return its area.

Examples
triArea(3, 2) ➞ 3

triArea(7, 4) ➞ 14

triArea(10, 10) ➞ 50
NOTE. AREA OF A TRIANGLE IS BASE * HEIGHT / 2 */
const findArea = (base, height) => {
  return (base * height) / 2;
};
console.log(`the area of triangle is ${findArea(7, 4)}`);
/*Create a function that takes an array containing only numbers and return the first element.

Examples
getFirstValue([1, 2, 3]) ➞ 1

getFirstValue([80, 5, 100]) ➞ 80

getFirstValue([-500, 0, 50]) ➞ -500*/
function findArray(arr) {
  return arr[0];
}
console.log(findArray([80, 5, 100]));
/*Write a function that converts hours into seconds.

Examples
howManySeconds(2) ➞ 7200

howManySeconds(10) ➞ 36000

howManySeconds(24) ➞ 86400*/
const howManySeconds = (hour1, hour2) => {
  const time = hour1
    ? hour1 * 60 * 60 + " minutes is 2 hours"
    : hour2 * 60 * 60 + " minutes is 10 hours";
  return time;
};
console.log(howManySeconds(2, undefined));
console.log(howManySeconds(undefined, 10));

const animalInventory = (chicken, cow) => {
  let chickenString = String(chicken);
  while (chickenString.length < 3) {
    chickenString = "0" + chickenString;
  }
  console.log(`${chickenString} chickens`);

  let cowString = String(cow);
  while (cowString.length < 3) {
    cowString = "0" + cowString;
  }
  console.log(`${cowString} cows`);
};
console.log(animalInventory(7, 11));

const myNiece = {
  firstNiece: "mitchelle, she is the eldest",
  secondNiece: "micayla , she is the youngest",
};
for (const niece in myNiece) {
  console.log(`${niece} is ${myNiece[niece]}`);
}
//create an object that takes a value and assign it to another object
const foodPreparation = {
  rice: "parboiled",
  beans: "steamed",
  garri: "diluted",
};
const foodPreparation2 = foodPreparation;
console.log(foodPreparation);
// Create an object that take a value called "coconut" and use a slice method to change it to nut, also indexof "u"
const coconut = {
  isFruit: "coconut",
};
console.log(coconut.isFruit.slice(2, 4));
console.log(coconut.isFruit.slice(4, 7));
const fiilIn = "green bug";
const fiilIn2 = fiilIn;
console.log(fiilIn2);

/* Task description: Write a method that makes a deep compare of two objects
 * Expected Result: True if objects are equal, false if objects are different */
function Compare(utensil, school) {
  this.utensil = utensil;
  this.school = school;
  this.checkCompare = function () {
    if (utensil === school) {
      return "utensill and school are the same";
    } else {
      return "never equal to each other";
    }
  };
}
const seeResult = new Compare("frying pan and spatuler", "school chalk");
console.log(seeResult.checkCompare());
/*Write a JavaScript program to display the reading status (i.e. display book name, author name and reading status) of 
the following books */
const library = [
  {
    bookTitle: "wolve bayne",
    aurthor: "migael swiden",
    readingStatus: true,
  },
  {
    bookTitle: "on baord gamers",
    aurthor: "coven dennis",
    readingStatus: true,
  },
  {
    bookTitle: "be warned",
    aurthor: "daniel schennieder",
    readingStatus: false,
  },
];
for (let read = 0; read < library.length; read++) {
  const allRead =
    " " +
    library[read].bookTitle +
    " " +
    "written by " +
    library[read].aurthor +
    "";
  if (library[read].readingStatus) {
    console.log("i have read " + allRead);
  } else {
    console.log("i need to read " + allRead);
  }
}
/*5. Write a JavaScript program to get the volume of a cylindrical with four decimal places using object classes.
Volume of a cylinder : V = πr2h
where r is the radius and h is the height of the cylinder*/
function DiameterRad(heigth, diameter) {
  this.heigth = heigth;
  this.diameter = diameter;
  this.radius = function () {
    return ((Math.PI * diameter) / 2) * heigth;
  };
}
const volume = new DiameterRad(4, 7);
console.log(`volume = ${volume.radius()}`);

//write a javascript method to print age for attending house parties for teennagers
const teenager = [
  {
    clubparties: "montell club",
    ageRating: 18,
    cluber: 16,
  },
  {
    clubparties: "s_no patrol gang club ",
    ageRating: 20,
    cluber: 21,
  },
  {
    clubparties: "denvil bitch club",
    ageRating: 18,
    cluber: 18,
  },
];
for (let age = 0; age < teenager.length; age++) {
  const order =
    " " +
    teenager[age].clubparties +
    "  " +
    "said u must be above " +
    teenager[age].ageRating +
    " " +
    "years to enter the club house";
  if (teenager[age].ageRating >= teenager[age].cluber) {
    console.log("manager for " + order);
  } else {
    console.log("can't enter manager " + order);
  }
}
/*write a javacsript code to strictly compare two keys of the same type for equality */
function value(key1, key2) {
  this.key1 = key1;
  this.key2 = key2;
  this.strict = function () {
    if (this.key1 === this.key2) {
      return "the object keys are equal";
    } else {
      return "the keys are not equal";
    }
  };
}
const equilibrum = new value("goat", "goat");
console.log(equilibrum.strict());
// OR
const toValue = [
  {
    key1: "chicken",
    key2: "chicken",
  },
];
for (let strict = 0; strict < toValue.length; strict++) {
  if (this.key1 === this.key2) {
    console.log("the keys are equal");
  } else {
    console.log("the are the same keys");
  }
}
/*write an object constructor to calculate for power where work and time are given */
function calculateForPower(time, work) {
  (this.work = work), (this.time = time);
  this.forPower = function () {
    return work / time / 2;
  };
}
const power = new calculateForPower(120, 35);
console.log(power.forPower() + "J.s_1");
/*write a physics problem to epress how momentum can be solved in javascript*/
function calculateForMomentum(mass, velocity) {
  (this.mass = mass),
    (this.velocity = velocity),
    (this.toCalculate = () => {
      return mass * velocity;
    });
}
const momentum = new calculateForMomentum(50, 13);
console.log(momentum.toCalculate() + "Kg.m/s_1");
/*the distance covered by a moving body is 350km and time taken by the body is 2hr.50min, calculate the speed ?*/
function toCalculateSpeed(distance, time) {
  (this.distance = distance),
    (this.time = time),
    (this.toCheckForSpeed = function () {
      return distance / time;
    });
}
const ForSpeed = new toCalculateSpeed(350, 2.5);
console.log(ForSpeed.toCheckForSpeed() + "Kmh_1");
/*a car travel from north to south at an avarage of 100khm_1 and the time covered was 5 min calculate for distance 
note : u will convert min to seconds*/
function toDistance(speed, time) {
  (this.speed = speed),
    (this.time = time),
    (this.checkOutput = function () {
      return (speed / 60) * time;
    });
}
const ThedistanceCovered = new toDistance(100, 5);
console.log(ThedistanceCovered.checkOutput() + "Km");
/*a body moving on it's velocity at 80km and end at a velocity of 40km and the time taken to cover the distance was 3hrs
calculate for the acceleration 
NOTE: respectively pay attention to the formular that says a = v-u/t */
function forAcceleration(initialVelocity, finalVelocity, time) {
  (this.initialVelocity = initialVelocity),
    (this.finalVelocity = finalVelocity),
    (this.time = time),
    (this.result = function () {
      return initialVelocity - finalVelocity / time;
    });
}
const acceleration = new forAcceleration(80, 40, 3);
console.log(acceleration.result() + "Ms_2");
//if a bus covers a distancse of 200km in two hours calculate the speed
function getSpeed(distance, time) {
  (this.distance = distance),
    (this.time = time),
    (this.speedCheck = () => {
      return distance / time;
    });
}
const outputOfSpeed = new getSpeed(200, 2);
console.log(`${outputOfSpeed.speedCheck()}Kmh_1`);
/*the velocity decreases from 10 to 0 over the period from 7 seconds to 12 seconds. the body is said to experience
retardation or deceleration along this line*/
function forDeceleration(
  initialVelocity,
  finalVelocity,
  initialTime,
  finalTime
) {
  (this.initialVelocity = initialVelocity),
    (this.finalVelocity = finalVelocity),
    (this.initialTime = initialTime),
    (this.finalTime = finalTime);
  this.retardationCalculation = function () {
    return initialVelocity - finalVelocity / finalTime - initialTime;
  };
}
const deceleration = new forDeceleration(10, 0, 7, 12);
console.log(deceleration.retardationCalculation() + "Ms_2");

/*if three resistance 2 ,3,5 are connected parallel in a circuit, calculate the equivalent resistance of the 
combination. formula 1/R = 1/R1 + 1/R2 + 1/R3*/
function calculateOnResistance(resistance1, resistance2, resistance3) {
  this.resistance1 = resistance1;
  this.resistance2 = resistance2;
  this.resistance3 = resistance3;
  this.outputResistance = function () {
    return (resistance1 * resistance2 * resistance3) / 31;
  };
}
const resistance = new calculateOnResistance(2, 3, 5);
console.log(resistance.outputResistance());
/*the head lamp of a car take a current of 0.4 ampere from a 12 volt supply. the energy produced in 5 minutes is ?*/
function Watt(current, volt, time) {
  (this.current = current),
    (this.volt = volt),
    (this.time = time),
    (this.Energy = function () {
      return current * volt * 60 * time;
    });
}
const energyProduced = new Watt(0.4, 12, 5);
console.log(energyProduced.Energy());
/*Write a JavaScript function called calculateArea that takes the length and width of a rectangle as parameters
 and returns its area. Use this function to calculate the area of a rectangle with a length of 5 units and a 
 width of 3 units. Store the result in a variable called rectangleArea and log it to the console.
*/
/*
function calculateTheArea(length,width) {
    this.length = length,
    this.width = width,
    this.area = function () {
        return length * width
    }
}
const rectangleArea = new calculateTheArea(5,3);
console.log(rectangleArea.area())*/
function calculateTheArea(length, width) {
  return length * width;
}
const rectangleArea = calculateTheArea(5, 3);
console.log(rectangleArea);
/*Create a program that prompts the user to enter their age and stores it in a variable called userAge. Write
 an if-else statement to check if the user is eligible to vote (age 18 or above). If they are eligible, display 
 "You are eligible to vote." Otherwise, display "You are not eligible to vote
 yet."*/
const enterName = (userAge) => {
  if (userAge >= 18) {
    return "you are eligible to vote";
  } else {
    return "sorry u must 18 years and above to vote ";
  }
};
console.log(enterName(prompt("please enter your age")));
/*Create an object called car with properties brand, model, and year. Prompt the user to enter the brand, model, and
  year of their car, and store the values in the corresponding properties of the car object. Finally, display the car's
   details using console.log().
   */
const carObject = {
  brand: prompt("put car brand please"),
  model: prompt("please fill in the car model"),
  years: prompt("cars year please"),
};
const propertiesOfTheCar = {
  ...carObject,
  nextModel: prompt("when will the next model be out"),
};
console.log(propertiesOfTheCar);

const game = {
  EAsports: "fifa mobile",
  konami: "winning eleven",
  microSoft: "ps5",
};
const createGamesYear = {
  ...game,
  EAYear: 2022,
  konamiYear: 2010,
  microSoftYear: 2000,
};
console.log(game);
console.log(createGamesYear);
/*create a copy of aa following object, adda new property to occupation and ensure tat the copied object contains
  all the information const user = {
    name : "john",
    age : 25,
    occupation :{
        role : "developer"
    }

  }*/
const userName = {
  name: "john",
  age: 25,
  occupation: {
    role: "developer",
  },
};
const copyUserName = {
  ...userName,
  occupation: {
    ...userName.occupation,
    specialization: "film making",
  },
};
console.log(copyUserName);

/*create an object called workerProfile and workersOrigin then then display the full details in another property
  then console.log() your result*/
const workersProfile = { id: 101, name: "david michael" };
const workersOrigin = { age: 34, country: "senegal" };
const workersFullDetails = { ...workersProfile, ...workersOrigin };
console.log(workersFullDetails);

//desrtucturing in object
let creatingAGamePlattform = {
  title: "titanic snipper",
  plattform: "SMe5",
  rating: "4.5 rating",
};
const { title, plattform, rating } = creatingAGamePlattform;
console.log(title, plattform, rating);

const destructArray = ["bird", "lion", "giraffe"];
const [bird, lion, giraffe] = destructArray;
console.log(bird, lion, giraffe);
/*create a function named goToSecondClass() that accept a destructured object as a paramter.the parameter extracts
the "secondHour" property of the object.the function should return this statement "time to go for ${secondHour} class*/
const myClass = {
  firstClass: "computer soft ware program",
  secondClass: "javascript programming",
};
function goToSecondClass({ secondClass }) {
  return `time to go for the ${secondClass} class`;
}
console.log(goToSecondClass(myClass));
/*write a function called displayFirstName() that takes an object as an argument and prints the persons first name and
last name. use object destructuring in the function argument and also use template strings when printing the first and 
the last name*/
const allMyNames = {
  firstName: "daniel",
  surname: "duru",
  lastName: "chiemela",
};
function displayFirstName({ firstName, lastName }) {
  return `my name is ${firstName} ${lastName}`;
}
console.log(displayFirstName(allMyNames));
/*Given two arguments, return an array which contains these two arguments.

Examples
makePair(1, 2) ➞ [1, 2]*/
function arrayReturn(firstNum, secondNum) {
  return [firstNum, secondNum];
}
console.log(arrayReturn(1, 2));
/*1. Write a JavaScript function to check whether an `input` is an array or not.
Test Data :
console.log(is_array('w3resource'));
false
true */
const testData = (input) => {
  if (toString.call(input) === "[object Array]") return true;
  return false;
};
console.log(testData("buggy man"));
console.log(testData([1, 2, 4, 0]));
const addEvent = [
  { day: "worked", dayEvent: true },
  { day: "touch tree", dayEvent: false },
  { day: "ate pizza", dayEvent: true },
  { day: "went for a television night", dayEvent: true },
  { day: "Run", dayEvent: false },
];
for (i = 0; i < addEvent.length; i++) {
  const detailsEvent = "within all these day event jacues " + addEvent[i].day;
  if (addEvent[i].dayEvent === true) {
    console.log(detailsEvent);
  } else {
    console.log("jacues didn't " + addEvent[i].day);
  }
}
/*Write a JavaScript function to get the first element of an array. Passing the parameter 'n' will return the first 
'n' elements of the array.
Test Data :
console.log(first([7, 9, 0, -2]));
console.log(first([],3));
console.log(first([7, 9, 0, -2],3));
console.log(first([7, 9, 0, -2],6));
console.log(first([7, 9, 0, -2],-3));
Expected Output */
function firstElement(elementArray, nElementOfTheArray) {
  for (i = 0; i < elementArray.length; i++) {
    if (elementArray === null) return [];
    if (nElementOfTheArray === undefined) return elementArray[0];
    if (nElementOfTheArray < 0) return elementArray[0];
  }
  /*if (elementArray === []) return void 0;
  if (nElementOfTheArray === null) return elementArray[0];
  if (nElementOfTheArray < 0) ;
  return elementArray.slice(0,nElementOfTheArray);*/
}
console.log(firstElement([7, 9, 0, -2], 3));
console.log(firstElement([7, 9, 0, -2]));
console.log(firstElement([], 3));
console.log(firstElement([7, 9, 0, -2], 6));
console.log(firstElement([7, 9, 0, -2], -3));
/*Write a JavaScript function to get the last element of an array. Passing the parameter 'n' will return the
 last 'n' elements of the array.
Test Data :
console.log(last([7, 9, 0, -2]));
console.log(last([7, 9, 0, -2],3));
console.log(last([7, 9, 0, -2],6));*/
function lastElement(elementArray, nElementOfTheArray) {
  if (nElementOfTheArray === null) return elementArray[3];
  if (nElementOfTheArray > 0) return nElementOfTheArray;
  //return nElementOfTheArray.slice(elementArray, 0);
}
console.log(lastElement([10, 3, 4, -5]));
console.log(lastElement([10, 3, 4, -5], 3));
console.log(lastElement([10, 3, 4, -5], 6));

/*Write a simple JavaScript program to join all elements of the following array into a string.

Expected Output:
"Red,Green,White,Black"
"Red,Green,White,Black"
"Red+Green+White+Black"*/
const joinColor = ["red", "green", "white", "black"];
console.log(joinColor.toString(""));
console.log(joinColor.join("+"));

const array = [1, 2, 3, 4, 5, 6];
const [a, , c, ...remaining] = array;

console.log(c);
const row_In_Number = [
  [1, 2, 1, 24],
  [8, 11, 9, 4],
  [7, 0, 7, 27],
  [7, 4, 28, 14],
  [3, 10, 26, 7],
];
const rowNumber = [0, 1, 2, 3, 4];
for (j in row_In_Number) {
  for (i in rowNumber) {
    const printRow = "row " + rowNumber[i] + " , " + row_In_Number[i];
    console.log(printRow);
  }
  break;
}
//OR
const row_arr = [
  [1, 2, 1, 24],
  [8, 11, 9, 4],
  [7, 0, 7, 27],
  [7, 4, 28, 14],
  [3, 10, 26, 7],
];
for (var i in row_arr) {
  console.log("row " + i);
  for (var j in row_arr[j]) {
    console.log("" + row_arr[i][j]);
  }
}

const friendsName = ["victor", "victoria", "gift", "emmanuel"];
const firstToLast = ["first ", "second ", "third", "last "];

for (i = 0; i < friendsName.length; i++) {
  for (x = 0; x < firstToLast.length; x++) {
    const concatFriends = firstToLast[x] + " friend is " + friendsName[x];
    console.log(concatFriends);
  }
  break;
}
const opponents = {
  firstOpponent: "ekindu",
  secondOpponent: "gregor",
  thirdOpponent: "masvidal",
};
for (let fight in opponents) {
  console.log(`  ${opponents[fight]} will fight today `);
}
/*
Write a JavaScript function to sort the following array of objects by title value.

Sample object:

var library = [ 
   { author: 'Bill Gates', title: 'The Road Ahead', libraryID: 1254},
   { author: 'Steve Jobs', title: 'Walter Isaacson', libraryID: 4264},
   { author: 'Suzanne Collins', title: 'Mockingjay: The Final Book of The Hunger Games', libraryID: 3245}*/
const getByLibrary = [
  { author: "bill gate", title: "the road ahead", libraryID: 1254 },
  { author: "steve jobs", title: "walter isaacson", libraryID: 4264 },
  {
    author: "suzanne collins",
    title: "mockingJay: the final book of the Hunger Game",
    libraryID: 3245,
  },
];
function sort_by_title(y, x) {
  if (y.author < x.author) return -1;
  if (y.author > x.author) return 1;
  return 0;
}
console.log(getByLibrary.sort(sort_by_title));

//Write a JavaScript function to find an array containing a specific element.and return true
function containing(arrayElement, element) {
  for (i = 0; i < arrayElement.length; i++) {
    if (arrayElement[i] === element) return element;
  }
  return false;
}
console.log(containing([2, 3, 5, 7, 6, 8], 8));

/*write a javascript function to find if an element in an array is equal to the other parameter
and return true*/
const equalized_element = (element1, element2) => {
  for (t = 0; t < element2.length; t++) {
    if (element1 === element2[t]) return true;
  }
  return false;
};
console.log(equalized_element("horse", ["horse", "pigeon", "rat"]));
//write a javasccript to get a random element of a concatenated array
function getRandomElement(arr1, arr2) {
  const concatArr = [...arr1, ...arr2];
  return concatArr[Math.floor(Math.random() * concatArr.length)];
}
const arr1 = [56, 67, 890, 1000];
const arr2 = [34, 678, 98, 762];
console.log(getRandomElement(arr1, arr2));

function getRandom(item) {
  return item[Math.floor(Math.random() * item.length)];
}
const item = [254, 45, 212, 365, 2543];
console.log(getRandom(item));

//write a javacsript to print last element of an array
const last_element = (element1, element2) => {
  if (element1 <= 0) return element2.slice(2, 3);
  if (element1 > 0) return element2[3];
};
console.log(last_element(23, [23, 4, 5, 78]));
console.log(last_element(0, [12, 3, 45]));

const array_sorting = [
  { name: "prisca", age: 20, level: "level1" },
  { name: "precious", age: 24, level: "level1" },
  { name: "michael", age: 19, level: "level3" },
];
function sort_age(age1, age2) {
  if (age1.age < age2.age) return -1;
}
console.log(array_sorting.sort(sort_age));
